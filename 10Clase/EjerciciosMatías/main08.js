const log = console.log;

let miArray = 'Alorem ipsum scammer simply laboris sit amet';

const aux = Validar(miArray);

log(aux);

function Validar(miArray)
{
    return miArray.charAt[miArray.lengh - 1] == 'b';

}

